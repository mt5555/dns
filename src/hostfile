#setenv LAMRSH "ssh -x"   does not seem to be needed?
# to compile with lf95 support:   ./configure --prefix=/contrib/lam --with-fflags=--staticlink
#lamboot -v hostfile  startup lam 
#tping N              will ping all mpi hosts
#mpitask              show status
#lamclean -v          kill running mpi program
#lamhalt              shut down lam

kiwi1
kiwi2
kiwi3
kiwi4
kiwi5



